{
  "name": "saga",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://chraltro.github.io/saga",
  "scripts": {
    "setup": "node scripts/setup-dev.js",
    "predev": "node scripts/setup-dev.js",
    "dev": "set ROLLUP_DISABLE_NATIVE=1&& vite",
    "build": "set ROLLUP_DISABLE_NATIVE=1&& vite build",
    "preview": "vite preview",
    "deploy": "npm run build && node scripts/publish-to-root.js && git add -A && git commit -m \"Deploy site\" || echo Skipping commit && git push"
  },
  "dependencies": {
    "@google/genai": "^1.22.0",
    "marked": "^16.4.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^4.2.1",
    "gh-pages": "^6.3.0",
    "rollup": "^3.29.4",
    "typescript": "~5.8.2",
    "vite": "^5.4.10"
  }
}
